<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="KhinkalyatorTheme" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/white_back</item> <!-- for preview -->
    </style>

    <style name="SplashTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/purple_button</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/KhinkalyatorTheme</item>
    </style>
</resources>